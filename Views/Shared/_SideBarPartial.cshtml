@functions {
    bool IsLinkActive(string controller, string action)
    {
        var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
        var currentController = ViewContext.RouteData.Values["controller"]?.ToString();

        if (string.Equals(currentAction, action, StringComparison.OrdinalIgnoreCase) &&
            string.Equals(currentController, controller, StringComparison.OrdinalIgnoreCase))
        {
    // Check for additional route values (parameters)
            var routeData = ViewContext.RouteData.Values;
            var routeValues = new RouteValueDictionary(routeData);

    // Exclude "action" and "controller" from the comparison
            routeValues.Remove("action");
            routeValues.Remove("controller");

    // If there are no remaining route values, the link is active
            return !routeValues.Any();
        }
        return false;
    }

}
<div class="main-menu menu-fixed menu-dark menu-accordion menu-shadow" data-scroll-to-active="true">
    <div class="main-menu-content">
        <ul class="navigation navigation-main" id="main-menu-navigation" data-menu="menu-navigation">
            <li class="nav-item @(IsLinkActive("Home", "Index") ? "active" : "")"><a asp-action="Index" asp-controller="Home">
                <i class="la la-dashboard"></i>
                <span class="menu-title">Dashboard</span>
            </a>
            <li class="nav-item">
                <a href="#"><i class="la la-industry"></i>
                    <span class="menu-title">PPE</span></a>
                <ul class="menu-content">
                    <li class="@(IsLinkActive("Categories", "Index") ? "active" : "")">
                        <a class="menu-item" asp-controller="Categories" asp-action="Index"><i></i>
                            <span>PPE Categories</span>
                        </a>
                    </li>
                    <li class="@(IsLinkActive("Ppes", "Index") ? "active" : "")">
                        <a class="menu-item" asp-controller="Ppes" asp-action="Index"><i></i>
                            <span>PPEs</span>
                        </a>
                    </li>
                </ul>
            </li>
            <li class="nav-item @(IsLinkActive("Projects", "Index") ? "active" : "")">
                <a asp-action="Index" asp-controller="Projects">
                    <i class="la la-list"></i>
                    <span class="menu-title">Projects</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="#"><i class="la la-list"></i>
                    <span class="menu-title">Stocks</span></a>
                <ul class="menu-content">
                    <li class="@(IsLinkActive("Stocks", "Index") ? "active" : "")">
                        <a class="menu-item" asp-controller="Stocks" asp-action="Index"><i></i>
                            <span>Stocks movements</span>
                        </a>
                    </li>
                    <li class="@(IsLinkActive("Stocks", "Index") ? "activee" : "")">
                        <a class="menu-item" asp-controller="Stocks" asp-action="Index"><i></i>
                            <span>Stock adjustment</span>
                        </a>
                    </li>
                </ul>
            </li>
            
            <li class=" nav-item">
                <a href="#"><i class="la la-cog"></i>
                    <span class="menu-title">Administration</span></a>
                <ul class="menu-content">
                    <li class="@(IsLinkActive("Functions", "Index") ? "active" : "")">
                        <a class="menu-item" asp-controller="Functions" asp-action="Index"><i></i>
                            <span>List functions</span>
                        </a>
                    </li>
                    <li class="@(IsLinkActive("Employees", "Index") ? "active" : "")">
                        <a class="menu-item" asp-controller="Employees" asp-action="Index">
                            <i></i>
                            <span>List employees</span>
                        </a>
                    </li>
                    <li class="@(IsLinkActive("Managers", "Index") ? "active" : "")">
                        <a class="menu-item" asp-controller="Managers" asp-action="Index">
                            <i></i>
                            <span>Mangers</span>
                        </a>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</div>